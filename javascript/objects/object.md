# Objects in JavaScript
objects in javascript used to store key:value pairs.
used to store related information
we can create objects in 4 ways 
 1)object literal 
 2)New Reference
 3)functional
 4)classes(ES6)
Object Methods in javascript
 1)call -> borrows the function from the other object
 2)Apply -> borrows the function from the other object but required to pass the arguements in the array
 3)bind -> borrows the function from the other object but it returns the bounding function instead of executing it directly